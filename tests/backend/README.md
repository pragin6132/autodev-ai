# Backend Tests Directory

Write python test files under this directory naming them starting with `test_*.py`.

---

## Example pytest usage:

Run standard tests:
```bash
pytest tests/backend/
```

Run checks verbose:
```bash
pytest -v tests/backend/
```
